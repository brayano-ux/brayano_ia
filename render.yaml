services:
  - type: web
    name: brayano-ai-backend
    runtime: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./apps/backend/Dockerfile
    dockerContext: ./apps/backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
